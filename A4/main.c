#include <stdio.h> 
#include <stdlib.h>
#include "buffer.h" 

#define MAX_DATA_SIZE 10

void processing(float local_buffer[]);

int main(void) {
  float local_buffer[MAX_DATA_SIZE + 2] = {0};

}
void processing(float local_buffer[]) {
}
/* The file buffer.c should contain the following functions (of course, the names of your
functions could be different): */
void generate_data() {}
int reading() {}
void writing() {}
void display_buffer() {} // a new function to display the content of the output buffer
